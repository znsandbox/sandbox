define([
    'vue',
    'vuex'
], function (
    Vue,
    Vuex
) {

    Vue.use(Vuex);

    /*return new Vuex.Store({
        state: {
            items: []
        },
        mutations: {
            setItems: function (state, items) {
                state.items = items;
                console.info(state.items);
            }
        },
        getters: {
            getItems: function (state) {
                return state.items;
            }
        }
    });*/

});